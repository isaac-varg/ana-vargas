export const headingText = 'Available for open positions and freelance projects';

export const largeText = `Got a creative project or open position that you think I'd be a great fit for?`

export const buttonText = `Let's chat`